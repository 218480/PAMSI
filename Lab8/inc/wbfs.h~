#ifndef guard_bfs
#define guard_bfs
#include "../inc/wgraph.h"
#include "../inc/queue.h"
class wbfs
{
private:
int dist=1;
bool visited(int*, int);
bool browse(wgraph, int, stack&, int*, int);
public:
stack search(wgraph, int, int);
};


#endif
