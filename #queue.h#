#ifndef guard_queue
#define guard_stack
#include "istack.h"

class stack : public istack
{
private:
  element *end=nullptr;
  int add(int, int);
  void remove(int);
  int get(int);
 public:
  int pop();
  void push(int);
  int size();

};

#endif
