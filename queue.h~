#ifndef guard_queue
#define guard_queue
#include "iqueue.h"
#include "list.h"

class queue : public iqueue, public list
{
private:
  int add(int, int);
  void remove(int);
  int get(int);
 public:
  int pop();
  void push(int);
  int size();

};

#endif
